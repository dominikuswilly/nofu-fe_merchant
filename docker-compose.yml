services:
  app:
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VUE_APP_BACKEND_URL: https://merchantdev.bengkelfajarjaya.com/api
        NODE_ENV: production
    image: app.netbird.cloud:8081/fe-merchant/stable:1.0.0
    ports:
      - "8080:8080"
    restart: unless-stopped
